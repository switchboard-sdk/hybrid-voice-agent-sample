# Switchboard SDK credentials
#
# scripts/postinstall.js copies this file to .env on install. Both values are
# blank: until you fill them in, the app opens on a screen saying so.
#
# Register for credentials at https://console.switchboard.audio/register

EXPO_PUBLIC_SWITCHBOARD_APP_ID=
EXPO_PUBLIC_SWITCHBOARD_APP_SECRET=

# Cloud brain
#
# Nothing to fill in. The cloud brain reaches an LLM through Switchboard's chat
# endpoint, which takes the app ID and secret above and keeps the provider key on
# the server — so no key is compiled into the app. What it needs instead is an
# OpenAI key on the app itself, added in the console: see "Cloud credentials" in
# the README.
#
# Optional. Defaults to https://api.switchboard.audio/chat. Point it elsewhere to
# reach the endpoint through a host of your own.

EXPO_PUBLIC_CLOUD_LLM_BASE_URL=

# On-device model
#
# Optional. Where the app fetches the language model on first launch. Defaults to
# a public mirror of the Llama 3.2 1B build the SDK's LLM extension carries. Point
# it at your own host to serve the weights yourself — see "The language model" in
# the README, which also covers what overriding it gives up.

EXPO_PUBLIC_LLM_MODEL_URL=

# Signing
#
# Your Apple Developer team ID, used when building for a device or for TestFlight.
# Not needed for the Simulator, and not compiled into the app: app.config.js reads
# it at build time only. Find it at https://developer.apple.com/account

APPLE_TEAM_ID=
